{
  "name": "shonchoy",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "$honchoy: savings & money planning for women (English / বাংলা). Static HTML/CSS/JS, no build step.",
  "scripts": {
    "dev:sandbox": "wrangler pages dev public --ip 0.0.0.0 --port 3000",
    "build": "echo 'No build step: static site lives in public/'",
    "test": "node --test tests/*.test.js",
    "deploy:cf": "wrangler pages deploy public"
  },
  "devDependencies": {
    "wrangler": "^4.110.0"
  }
}
